# .env.example — nama variabel saja (USULAN, tanpa nilai, tanpa secret)
# Shared concern (TEAM_WORKFLOW.md:36-41): butuh agree frontend sebelum dipakai.
# SERVICE_ROLE TIDAK PERNAH ke frontend (ARCHITECTURE.md:37-39). Hanya server-side.

SUPABASE_URL=
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=  # server-side only, via secret manager, JANGAN commit nilainya
